class Solution
    def __init__(self)
        self.answer=[]
    def generate(self,opens int,closes int,total int ,now  str)
        if(openstotal)
            self.generate(opens+1,closes,total,now+()
        if(openscloses)
            self.generate(opens,closes+1,total,now+))
        if(opens==total and closes==total)
            self.answer.append(now)
            
    def generateParenthesis(self, n int) - List[str]
        self.generate(0,0,n,)
        return self.answer
